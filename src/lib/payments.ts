/* Constantes de paiement partagées entre composants client (montant à
   afficher/faire payer par le widget) et actions serveur (montant à
   vérifier auprès de KKiaPay) — un seul chiffre à changer si le tarif
   évolue. */

/** Frais d'une demande de visite, en FCFA. */
export const VISIT_FEE_FCFA = 200;
